tags
test.sh
.luarc.json
nvim

spell/

# Track lazy-lock.json in personal dotfiles so plugin versions are reproducible
# on a fresh machine. Runtime files belong in .gitignore; lockfiles document
# the exact plugin commits that are known to work.

.DS_Store
.nvimlog
